# Rust toolchain binaries live in GitHub Releases, not in git
*.tar.xz
*.tar.gz
*.exe
*.msi
*.vsix
rust-*-windows-*/
# cargo `vendor/` output (only if this repo ever gains a cargo project;
# the vendor/*.md docs below MUST stay tracked, so this stays commented)
# vendor/
target/
.vs/
# local cargo/rustup homes if ever created here
.cargo/
.rustup/
